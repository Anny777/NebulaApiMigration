version: '3'

services:
    mssql:
        image: mcr.microsoft.com/mssql/server:2017-latest-ubuntu
        ports:
            - 1433:1433

    web-api:
        build: ./Dockerfile-web-api
        ports:
            - 5001:80
        depends_on:
            - mssql